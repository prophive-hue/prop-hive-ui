# Development Guide\n\n## Getting Started\n\n### Prerequisites\n- Node.js 18 or higher\n- Angular CLI 19.2.8+\n- Git\n- VS Code (recommended)\n\n### Recommended VS Code Extensions\n- Angular Language Service\n- TypeScript Importer\n- Prettier - Code formatter\n- ESLint\n- Auto Rename Tag\n- Bracket Pair Colorizer\n\n### Project Setup\n```bash\n# Clone and setup\ngit clone <repository-url>\ncd prop-hive-ui\nnpm install\n\n# Start development\nng serve\n```\n\n## Architecture Guidelines\n\n### Component Structure\n\n#### Smart Components\n```typescript\n/**\n * Smart component for user management\n * Handles data fetching, state management, and business logic\n */\n@Component({\n  selector: 'app-user-list',\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class UserListComponent extends SmartComponent implements OnInit {\n  trackById = TrackByFunctions.trackById;\n  users: User[] = [];\n\n  ngOnInit() {\n    this.loadUsers();\n  }\n\n  private loadUsers() {\n    this.setLoading(true);\n    this.userService.getUsers()\n      .pipe(takeUntil(this.destroy$))\n      .subscribe({\n        next: users => {\n          this.users = users;\n          this.setLoading(false);\n        },\n        error: error => this.handleError(error)\n      });\n  }\n}\n```\n\n#### Presentation Components\n```typescript\n/**\n * Presentation component for user display\n * Only handles UI rendering and Input/Output communication\n */\n@Component({\n  selector: 'app-user-card',\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class UserCardComponent extends PresentationComponent {\n  @Input() user!: User;\n  @Output() userClick = new EventEmitter<User>();\n\n  onUserClick() {\n    this.userClick.emit(this.user);\n  }\n}\n```\n\n### Service Pattern\n\n#### Repository Services\n```typescript\n/**\n * User repository for API communication\n * Extends BaseHttpService for consistent HTTP handling\n */\n@Injectable({ providedIn: 'root' })\nexport class UserRepository extends BaseHttpService {\n  \n  /**\n   * Get paginated user list\n   * @param pagination - Pagination parameters\n   * @returns Observable of paginated users\n   */\n  getUsers(pagination: PaginationRequest): Observable<PaginatedResponse<User>> {\n    return this.get<PaginatedResponse<User>>('/users', pagination);\n  }\n\n  /**\n   * Create new user\n   * @param user - User data to create\n   * @returns Observable of created user\n   */\n  createUser(user: CreateUserRequest): Observable<User> {\n    return this.post<User>('/users', user);\n  }\n}\n```\n\n### Form Validation\n\n#### Security Validators\n```typescript\n// Form with security validators\nthis.userForm = this.formBuilder.group({\n  name: ['', [\n    Validators.required,\n    SecurityValidators.alphanumeric(),\n    SecurityValidators.noXss(),\n    SecurityValidators.noWhitespace()\n  ]],\n  email: ['', [\n    Validators.required,\n    Validators.email,\n    SecurityValidators.noXss(),\n    SecurityValidators.noSqlInjection()\n  ]],\n  password: ['', [\n    Validators.required,\n    SecurityValidators.strongPassword()\n  ]]\n});\n```\n\n#### Input Sanitization\n```html\n<!-- Automatic input sanitization -->\n<input \n  formControlName=\"email\"\n  appSanitizeInput \n  [sanitizeOptions]=\"{type: 'email'}\"\n/>\n\n<input \n  formControlName=\"name\"\n  appSanitizeInput \n  [sanitizeOptions]=\"{alphanumericOnly: true}\"\n/>\n```\n\n## Performance Best Practices\n\n### Change Detection\n```typescript\n// Always use OnPush strategy\n@Component({\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\n\n// Use trackBy functions in ngFor\n<div *ngFor=\"let item of items; trackBy: trackById\">\n```\n\n### Lazy Loading\n```html\n<!-- Lazy load images -->\n<img appLazyLoad=\"{{imageUrl}}\" alt=\"Description\" />\n\n<!-- Virtual scrolling for large lists -->\n<div appVirtualScroll=\"items\" [itemHeight]=\"50\">\n  <div *ngFor=\"let item of items\">{{item.name}}</div>\n</div>\n```\n\n### HTTP Optimization\n```typescript\n// Automatic caching via interceptor\n// GET requests cached for 5 minutes\n\n// Manual cache control\nthis.performanceService.cacheResponse('users', userData, 300000);\nconst cached = this.performanceService.getCachedResponse('users');\n```\n\n## Security Guidelines\n\n### Input Validation\n```typescript\n// Always validate user input\nSecurityValidators.strongPassword()     // Password complexity\nSecurityValidators.noXss()             // XSS prevention\nSecurityValidators.noSqlInjection()    // SQL injection protection\nSecurityValidators.phoneNumber()       // Phone validation\nSecurityValidators.alphanumeric()      // Alphanumeric only\n```\n\n### File Upload Security\n```typescript\n// Validate file uploads\nSecurityValidators.allowedFileTypes(['.jpg', '.png', '.pdf'])\nSecurityValidators.maxFileSize(5 * 1024 * 1024) // 5MB limit\n```\n\n## Error Handling\n\n### Component Level\n```typescript\n// Smart component error handling\nthis.userService.getUsers()\n  .pipe(takeUntil(this.destroy$))\n  .subscribe({\n    next: users => this.users = users,\n    error: error => this.handleError(error) // Automatic error handling\n  });\n```\n\n### Service Level\n```typescript\n// Service error handling\ncatch(error => {\n  const appError = this.errorHandler.handleHttpError(error);\n  this.errorHandler.displayError(appError);\n  return this.errorHandler.throwError(appError);\n})\n```\n\n## Testing Guidelines\n\n### Unit Tests\n```typescript\ndescribe('UserListComponent', () => {\n  let component: UserListComponent;\n  let fixture: ComponentFixture<UserListComponent>;\n  let userService: jasmine.SpyObj<UserService>;\n\n  beforeEach(() => {\n    const spy = jasmine.createSpyObj('UserService', ['getUsers']);\n    \n    TestBed.configureTestingModule({\n      imports: [UserListComponent],\n      providers: [\n        { provide: UserService, useValue: spy }\n      ]\n    });\n    \n    fixture = TestBed.createComponent(UserListComponent);\n    component = fixture.componentInstance;\n    userService = TestBed.inject(UserService) as jasmine.SpyObj<UserService>;\n  });\n\n  it('should load users on init', () => {\n    const mockUsers = [{ id: '1', name: 'Test User' }];\n    userService.getUsers.and.returnValue(of(mockUsers));\n    \n    component.ngOnInit();\n    \n    expect(userService.getUsers).toHaveBeenCalled();\n    expect(component.users).toEqual(mockUsers);\n  });\n});\n```\n\n## Code Style\n\n### TypeScript\n- Use strict TypeScript configuration\n- Prefer interfaces over types for object shapes\n- Use enums for constants\n- Always specify return types for public methods\n\n### Angular\n- Use OnPush change detection\n- Implement OnDestroy for subscription cleanup\n- Use reactive forms over template-driven forms\n- Prefer standalone components\n\n### Naming Conventions\n- Components: PascalCase (UserListComponent)\n- Services: PascalCase with Service suffix (UserService)\n- Files: kebab-case (user-list.component.ts)\n- Variables: camelCase (userName)\n- Constants: UPPER_SNAKE_CASE (MAX_FILE_SIZE)\n\n## Debugging\n\n### Performance Monitoring\n```typescript\n// Track component performance\nconst tracker = this.performanceService.trackPerformance('UserList', 'loadUsers');\n// ... operation ...\ntracker(); // Logs execution time\n```\n\n### Error Logging\n```typescript\n// Structured error logging\nconsole.group('🚨 VALIDATION_ERROR - MEDIUM');\nconsole.error('Message:', error.message);\nconsole.error('Code:', error.code);\nconsole.error('Timestamp:', error.timestamp);\nconsole.groupEnd();\n```\n\n## Deployment\n\n### Build Optimization\n```bash\n# Production build with optimization\nng build --configuration=production\n\n# Analyze bundle size\nnpm run analyze\n\n# Test production build locally\nng serve --configuration=production\n```\n\n### Environment Configuration\n```typescript\n// Environment-specific settings\nexport const environment = {\n  production: true,\n  apiUrl: 'https://api.production.com',\n  features: {\n    enableNewFeature: true,\n    enableDebugMode: false\n  }\n};\n```", "oldStr": ""}]