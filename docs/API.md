# API Documentation\n\n## Base Configuration\n\n**Base URL**: `https://ylkgde9us8.execute-api.eu-west-1.amazonaws.com`\n- **Development**: `/dev`\n- **Production**: `/prod`\n\n## Authentication\n\nAll authenticated endpoints require a Bearer token in the Authorization header:\n```\nAuthorization: Bearer <jwt-token>\n```\n\n## Endpoints\n\n### Authentication\n\n#### POST /auth/login\nAuthenticate user and receive JWT token.\n\n**Request Body:**\n```typescript\n{\n  email: string;     // Valid email address\n  password: string;  // Minimum 6 characters\n}\n```\n\n**Response:**\n```typescript\n{\n  token: string;\n  refreshToken: string;\n  user: {\n    id: string;\n    email: string;\n    name: string;\n    role: 'ADMIN' | 'INVESTOR' | 'GUEST';\n  }\n}\n```\n\n#### POST /auth/register\nRegister new user account.\n\n**Request Body:**\n```typescript\n{\n  name: string;      // Alphanumeric only\n  surname: string;   // Alphanumeric only\n  email: string;     // Valid email\n  phone: string;     // Valid phone number\n  password: string;  // Strong password required\n}\n```\n\n### Properties\n\n#### GET /properties\nRetrieve paginated property list.\n\n**Query Parameters:**\n- `page`: number (default: 0)\n- `size`: number (default: 10, max: 100)\n- `propertyName`: string (optional search filter)\n\n**Response:**\n```typescript\n{\n  content: Property[];\n  totalElements: number;\n  totalPages: number;\n  size: number;\n  number: number;\n}\n```\n\n#### POST /properties\nCreate new property (Admin only).\n\n**Request Body:**\n```typescript\n{\n  title: string;\n  location: string;\n  category: PropertyCategory;\n  description: string;\n  developer: string;\n  expectedRoi: number;\n  totalInvestment: number;\n  status: PropertyStatus;\n  base64Images: Base64File[];\n}\n```\n\n### Users Management\n\n#### GET /admin/investors\nRetrieve investor list (Admin only).\n\n**Query Parameters:**\n- `page`: number\n- `size`: number\n- `investorName`: string (optional)\n\n#### GET /admin/developers\nRetrieve developer list (Admin only).\n\n**Query Parameters:**\n- `page`: number\n- `size`: number\n- `companyName`: string (optional)\n\n### Statistics\n\n#### GET /admin/stats/investors\nRetrieve investor statistics (Admin only).\n\n**Response:**\n```typescript\n{\n  totalActive: number;\n  totalInvested: number;\n  totalPending: number;\n}\n```\n\n## Error Responses\n\nAll endpoints return standardized error responses:\n\n```typescript\n{\n  message: string;        // User-friendly error message\n  type: ErrorType;        // Error classification\n  severity: ErrorSeverity; // Error severity level\n  code?: string;          // Error code\n  timestamp: Date;        // Error timestamp\n}\n```\n\n### HTTP Status Codes\n- `200`: Success\n- `400`: Bad Request (validation errors)\n- `401`: Unauthorized (authentication required)\n- `403`: Forbidden (insufficient permissions)\n- `404`: Not Found\n- `500`: Internal Server Error\n\n## Security Headers\n\nAll requests include security headers:\n- `X-Content-Type-Options: nosniff`\n- `X-Frame-Options: DENY`\n- `X-XSS-Protection: 1; mode=block`\n\n## Rate Limiting\n\n- **Authentication endpoints**: 5 requests per minute\n- **General API**: 100 requests per minute\n- **File uploads**: 10 requests per minute\n\n## Data Validation\n\nAll input data is validated and sanitized:\n- XSS protection on all string inputs\n- SQL injection prevention\n- File type and size validation\n- Strong password requirements\n- Phone number format validation\n\n## Caching\n\n- **GET requests**: Cached for 5 minutes\n- **Static resources**: Cached for 24 hours\n- **User data**: No caching for sensitive information", "oldStr": ""}]